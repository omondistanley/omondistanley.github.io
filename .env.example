# Example environment variables for portfolio-react
# Copy this file to `.env.local` for local dev or `.env.production` for production

# Google Analytics 4 Measurement ID (optional)
VITE_GOOGLE_ANALYTICS_KEY=G-XXXXXXXXXX

# JuaCode embed URL
# For local development, point to your running JuaCode dev server
# For production, set to your hosted JuaCode site (Netlify, custom domain, etc.)
VITE_JUACODE_URL=http://localhost:3000
